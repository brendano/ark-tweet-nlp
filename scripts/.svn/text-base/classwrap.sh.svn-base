#!/bin/bash

# Set up classpath and invoke 'java' with it

here=$(dirname $0)

# eclipse dir
CP=$here/../bin

# our own build dir
CP=$CP:$here/../mybuild

for f in $(dirname $0)/../lib/*.jar; do
  CP=$CP:$f
done

set -x
exec java -cp "$CP" "$@"

